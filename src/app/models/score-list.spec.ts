import { ScoreList } from './score-list';
import { Player } from './player';
import { Game } from './game';

describe('ScoreList', () => {
  it('should create an instance', () => {
    expect(new ScoreList()).toBeTruthy();
  });
});
